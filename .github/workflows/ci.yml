name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: Node ${{ matrix.node }} on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest]
        node: ["22.12", "24"]
        # The manifest claims Windows support, so the suite runs there to keep that a tested claim:
        # path handling, PATH probing and the config location all differ, and each fails quietly.
        # One Node version is enough, since what differs on Windows is the OS, not V8.
        include:
          - os: windows-latest
            node: "24"

    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: ${{ matrix.node }}
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Audit dependencies
        if: matrix.os == 'ubuntu-latest' && matrix.node == '24'
        run: npm audit --audit-level=high

      # Formatting and lint are platform-independent, so one platform is enough to enforce them.
      - name: Check formatting and lint
        if: matrix.os == 'ubuntu-latest'
        run: npm run check

      - name: Build
        run: npm run build

      - name: Test
        run: npm test
